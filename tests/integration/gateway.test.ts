import { describe, it } from 'vitest';
import fetch from 'node-fetch';

describe('integration gateway', () => {
  it('skipped by default', async () => {
    // This test requires docker-compose env; keep as placeholder.
  });
});
